{
  "name": "certcheck-api",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build:dev": "wrangler deploy --dry-run --outdir=dist --env dev",
    "build:demo": "wrangler deploy --dry-run --outdir=dist --env demo",
    "build:prod": "wrangler deploy --dry-run --outdir=dist --env prod",
    "build": "wrangler deploy --dry-run --outdir=dist --env dev",
    "lint": "tsc --noEmit",
    "test": "vitest run",
    "check:flags": "bash scripts/check-flags.sh",
    "check:migrations": "bash scripts/check-migrations.sh"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/usmanahmedcertx-wq/certcheck-api.git"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "bugs": {
    "url": "https://github.com/usmanahmedcertx-wq/certcheck-api/issues"
  },
  "homepage": "https://github.com/usmanahmedcertx-wq/certcheck-api#readme",
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260920.1",
    "typescript": "^7.0.2",
    "vitest": "^5.0.1",
    "wrangler": "^4.135.0"
  }
}
